{
  "name": "logsupdate-tracker",
  "version": "1.0.0",
  "description": "Track and display HAVE_DONE.md work logs",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "nodemon server/server.js",
    "build": "cd client && npm run build",
    "client": "cd client && npm run dev"
  },
  "dependencies": {
    "better-sqlite3": "^9.2.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
